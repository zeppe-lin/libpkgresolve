# Contributing

Changes must preserve the authority boundaries in `DESIGN.md`.

Submit focused commits with tests for every new invariant. Do not add collection
scanning, YAML parsing, planner ordering, package execution, filesystem
mutation, historical compatibility, or untyped dependency strings to the core.

All files use GPL-3.0-or-later SPDX identifiers and the existing Alexandr Savca
author identity.
